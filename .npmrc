# Refuse to install with an npm other than the exact version pinned in package.json (engines.npm).
# Different npm versions write different package-lock.json metadata, so an install with the
# wrong npm rewrites the lock file and fails the CI freshness check.
engine-strict=true
